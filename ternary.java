public class ternary {
    public static void main (String args []){
         int x = 5, y = 3;
        //String result = (a > b) ? "A is greater" : "B is gtreater";
        //System.out.println(result);
        int min = (x < y) ? x:y;
        System.out.println("Minimum is :" + min);
         }
    
}
